# fb-iptc-remover

### Usage

```
fb_iptc_remover.py [-h] [-r] [--random-replace] [-v] rootdir

positional arguments:
  rootdir

optional arguments:
  -h, --help        show this help message and exit
  -r, --recursive
  --random-replace
  -v, --verbose

```
